apiVersion: v1
kind: ConfigMap
metadata:
  name: astra-config
data:
  database-url: "jdbc:postgresql://postgres-service:5432/astra_db"
  database-name: "astra_db"
  chroma-host: "chroma-service"
  chroma-port: "8000"
  redis-host: "redis-service"
  redis-port: "6379"
